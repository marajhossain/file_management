{% extends 'admin.html.twig' %}

{% block title %}Edit Form{% endblock %}

{% block body %}
    <div class="col-lg-12">
        <h1 class="page-header">Edit From</h1>
    </div>

    {{ include('form/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('form_index') }}" class="text-danger"> <i class="fa fa-arrow-left"></i> back to list</a>

{% endblock %}
